{
  "name": "@thmanyah/config",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:packages": "tsc -p tsconfig.json",
    "check-types": "tsc --noEmit",
    "dev": "pnpm build --watch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo node_modules .turbo"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.7"
  }
}
